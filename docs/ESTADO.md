# Estado

- **Versión**: 0.1.0 (sin publicar todavía).
- **Qué hay**: esqueleto del plugin, ajustes (origen + token con almacén provisional en `data.json`),
  botón de prueba de conexión, `LumbreClient.ping()` con tests, gate `npm run check`, CI y workflow
  de release para BRAT con los tres assets sueltos.
- **Qué falta**: el `.npmrc` con `tag-version-prefix=""` y `engine-strict=true` (ver README), las
  funciones de producto y la decisión de dónde vive el token; las tareas están en la lista
  `lumbre-obsidian` de Lumbre, no aquí.
